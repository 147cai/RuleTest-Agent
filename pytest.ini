[pytest]
testpaths = tests
asyncio_mode = auto

markers =
    network: marks tests that require external network access (httpbin.org)
    e2e: marks end-to-end tests that require the full server stack running
